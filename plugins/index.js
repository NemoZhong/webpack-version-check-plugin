const { VersionBuildPlugin, VERSION } = require('./version-build.js');
const VersionUpdateCheck = require('./version-check.js');

module.exports = {
  VersionBuildPlugin,
  VersionUpdateCheck,
  VERSION,
};
